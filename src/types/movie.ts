export type MovieForm = {
	genre: string;
	title: string;
	releasedAt: string;
	endAt: string;
};

export type MoiveData = {
	createdAt: string;
	endAt: string;
	genre: string;
	id: number;
	isShowing: boolean;
	releasedAt: string;
	title: string;
	updatedAt: string;
};
